#copy file from one file to another

echo enter a file name
read file

if test -f $file
then
echo file exist
echo provide name of the new file
read newfile
mv $file $newfile
echo new file content is
cat $newfile
else
echo file does not exist
fi
